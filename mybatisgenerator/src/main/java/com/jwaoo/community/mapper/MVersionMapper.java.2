package com.jwaoo.community.mapper;

import com.jwaoo.community.model.MVersion;
import com.jwaoo.community.model.MVersionExample;
import java.util.List;
import org.apache.ibatis.annotations.Param;
import org.apache.ibatis.session.RowBounds;

public interface MVersionMapper {
    long countByExample(MVersionExample example);

    int deleteByExample(MVersionExample example);

    int insert(MVersion record);

    int insertSelective(MVersion record);

    List<MVersion> selectByExampleWithRowbounds(MVersionExample example, RowBounds rowBounds);

    List<MVersion> selectByExample(MVersionExample example);

    int updateByExampleSelective(@Param("record") MVersion record, @Param("example") MVersionExample example);

    int updateByExample(@Param("record") MVersion record, @Param("example") MVersionExample example);
}